# Compiled command output (build artifact — regenerated by setup-scripts/compile-procedures.py)
output/

# Python bytecode from the compile/install scripts
__pycache__/

# Local dev environment (CI resolves fresh via `uv sync`, same as the memory core)
.venv/
uv.lock
.pytest_cache/
