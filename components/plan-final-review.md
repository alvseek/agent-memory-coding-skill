# Plan Final Review (plan component)

Final consistency check before presenting a plan for approval. **This is a component, not a standalone skill** — a wizard's "Final Review" step invokes it by reading and following this file.

*Consumed by `/high-wizard`, `/forge-of-covenant`, `/rite-of-creation`, `/pixel-wizard`. `/council-of-wizards` presents its sub-plan set differently and keeps its own.*

---

Before presenting the plan, double check: are there any unresolved decisions, assumptions, or new concerns that surfaced during writing or self-review that need [USER-NAME]'s input? If yes, present them now with the same decision format (options + confidence + reason).

Present the complete plan file link to [USER-NAME] for final review.
